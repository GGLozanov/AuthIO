<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools">

    <!--FIXME: fix this cheaty architecture and use only one variable
        FIXME (while keeping LiveData support from ViewModel and passing in immutable user instance for reading)-->
    <data>
        <variable
            name="viewmodel"
            type="com.example.authio.viewmodels.ProfileFragmentViewModel" />
        <variable
            name="immutableUser"
            type="com.example.authio.models.User" /> <!--Used when setting ListView  after being fetched by network-->
    </data>

    <LinearLayout
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="20dp"
        android:layout_marginBottom="20dp"
        android:background="@drawable/round_container"
        android:gravity="center"
        android:orientation="vertical"
        android:padding="15dp">

        <TextView
            android:id="@+id/username_text"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="center"
            android:layout_margin="10dp"
            android:gravity="center"
            android:padding="15dp"
            android:text="@{immutableUser.username ?? viewmodel.user.username}"
            android:textSize="18sp"
            android:textStyle="bold" />

        <ImageView
            android:id="@+id/profile_image"
            android:layout_width="300dp"
            android:layout_height="300dp"
            android:layout_gravity="center"
            android:layout_margin="10dp"
            android:cropToPadding="true"
            android:scaleType="centerCrop"
            android:visibility="visible"
            app:srcCompat="@drawable/default_img"
            tools:src="@drawable/default_img"
            tools:visibility="visible" />

        <TextView
            android:id="@+id/description_text"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="center"
            android:layout_margin="10dp"
            android:gravity="center"
            android:padding="15dp"
            android:text="@{immutableUser.description ?? viewmodel.user.description}"
            android:textSize="18sp"
            android:textStyle="bold" />

        <TextView
            android:id="@+id/email_text"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="center"
            android:layout_margin="10dp"
            android:gravity="center"
            android:padding="15dp"
            android:text="@{immutableUser.email ?? viewmodel.user.email}"
            android:textSize="18sp"
            android:textStyle="bold" />

        <Button
            android:id="@+id/logout_button"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:background="@drawable/round_button"
            android:drawableStart="@drawable/ic_exit_to_app_white_24dp"
            android:drawableLeft="@drawable/ic_exit_to_app_white_24dp"
            android:padding="10dp"
            android:text="@string/logout"
            android:textSize="18sp" />
    </LinearLayout>

</layout>